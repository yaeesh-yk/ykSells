const router = require('express').Router(); const c = require('../controllers/adminController'); const { verifyToken, isAdmin } = require('../middleware/authMiddleware'); router.use(verifyToken, isAdmin); router.get('/stats', c.stats); router.get('/revenue-chart', c.revenueChart); router.get('/users', c.users); router.delete('/users/:id', c.removeUser); router.get('/orders', c.allOrders); router.put('/orders/:id/status', c.status); module.exports = router;