const router = require('express').Router(); const c = require('../controllers/productController'); const { verifyToken, isAdmin } = require('../middleware/authMiddleware'); const upload = require('../middleware/upload'); const admin = [verifyToken, isAdmin]; router.get('/featured', c.featured); router.get('/', c.list); router.get('/:id', c.getOne); router.post('/', admin, upload.array('images', 8), c.create); router.put('/:id', admin, upload.array('images', 8), c.update); router.delete('/:id', admin, c.remove); router.post('/:id/review', verifyToken, c.review); router.delete('/:id/review', verifyToken, c.deleteReview); module.exports = router;